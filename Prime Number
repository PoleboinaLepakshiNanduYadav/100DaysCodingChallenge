// Online C compiler to run C program online
#include <stdio.h>

int main() {
   int num,i,c=0;
   printf("enter the num:");
   scanf("%d",&num);
   for(i=1;i<=num;i++)
   {
       if(num%i==0)
       {
           c++;
       }
   }
   if(c==2)
       {
           printf("prime num");
       }
       else
       {
           printf("Not prime num");
       }
}
